import React from "react";
import { createRoot } from "react-dom/client";
import InteriorAdsMindmap from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InteriorAdsMindmap />
  </React.StrictMode>,
);
